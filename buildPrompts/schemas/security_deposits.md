# security_deposits

## Description

PURPOSE: Manage holding and refunding of security deposits for rentals.

FIELDS:
id: string (PK)
rental_id: string (FK to rentals)
user_id: string (FK to renter)
amount: number (Value in base currency)
status: string (enum: pending, held, refunded, forfeited)
transaction_ref: string (Payment gateway reference)
createdAt: timestamp
updatedAt: timestamp

SECURITY RULES:
- Read: owner == auth.uid || renter == auth.uid || role == 'admin'
- Update: role == 'admin'

---
Generated by VisualPRD
