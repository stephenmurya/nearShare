# Build Step 001: Project Setup & Firebase Integration

## Task Description
Initialize the Near Share Flutter project and configure core infrastructure (Firebase, Theme, and Dependencies).

## Discovery
Read these first:
- [000.md](file:///c:/Users/USER/Documents/dev/nearShare/buildPrompts/000.md) (Project Overview)
- [tech-stack/flutter.md](file:///c:/Users/USER/Documents/dev/nearShare/buildPrompts/tech-stack/flutter.md)
- [design-system.md](file:///c:/Users/USER/Documents/dev/nearShare/buildPrompts/design-system.md)

## PROMPT
Initialize a new Flutter project named `near_share`. 
1. Setup the basic folder structure: `lib/core`, `lib/features`, `lib/shared`.
2. Configure `pubspec.yaml` with the following dependencies:
   - `firebase_core`, `firebase_auth`, `cloud_firestore`, `firebase_storage`
   - `google_sign_in`
   - `provider` or `riverpod` for state management
   - `go_router` for navigation
   - `intl`, `uuid`
3. Implement the `ThemeData` based on the [Design System](file:///c:/Users/USER/Documents/dev/nearShare/buildPrompts/design-system.md).
4. Create a `FirebaseService` class to initialize Firebase and provide access to Auth and Firestore.
5. Setup a basic `main.dart` that initializes Firebase and sets up the root `MaterialApp` with the brand theme.

## SUCCESS CRITERIA
- Project builds on Android/iOS without errors.
- Firebase initializes successfully on app start.
- `MaterialApp` reflects the primary/secondary colors from the design system.

## VERIFICATION PROMPT
"Simulate a run of the app and verify that Firebase initialization is logged. Check the Theme colors against the design system. Provide a mock of the folder structure."

---
Generated by VisualPRD
